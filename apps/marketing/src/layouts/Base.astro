---
// Fonts imported from JS so Vite bundles + fingerprints the woff2 files
// (a CSS @import leaves the relative url()s unresolved).
import "@fontsource-variable/geist";
import "@fontsource-variable/geist-mono";
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Rigel: run Kubernetes without an ops team",
  description = "Rigel is an AI-native desktop app for macOS that connects to your clusters, diagnoses issues, monitors them around the clock, and applies the fixes you approve.",
} = Astro.props;

const canonical = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Dark canvas on first paint, so reloads don't flash white before CSS. -->
    <meta name="color-scheme" content="dark" />
    <!-- Flag JS synchronously so the scroll-reveal start state never flashes
         (and content stays visible when JS is unavailable). -->
    <script is:inline>
      document.documentElement.classList.add("js");
    </script>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonical} />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0c0d0f" />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content="/og.svg" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="/og.svg" />

    <!-- Privacy-friendly analytics by Plausible. is:inline so Astro leaves the
         third-party script untouched (exact src + async preserved). -->
    <script
      is:inline
      async
      src="https://plausible.tybit.luxe/js/pa-amzJ-xZreTn2y6Xc2mkeE.js"></script>
    <script is:inline>
      window.plausible =
        window.plausible ||
        function () {
          (plausible.q = plausible.q || []).push(arguments);
        };
      plausible.init =
        plausible.init ||
        function (i) {
          plausible.o = i || {};
        };
      plausible.init();
    </script>
  </head>
  <body>
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-accent focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-fg-inverse"
    >
      Skip to content
    </a>
    <slot />

    <!-- Reveal-on-scroll. Runs after the DOM is parsed (placed at body end);
         reduced-motion or no IntersectionObserver → everything shown at once. -->
    <script is:inline>
      (function () {
        var els = document.querySelectorAll("[data-reveal]");
        var reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        if (reduce || !("IntersectionObserver" in window)) {
          els.forEach(function (el) {
            el.classList.add("is-visible");
          });
          return;
        }
        var io = new IntersectionObserver(
          function (entries) {
            entries.forEach(function (e) {
              if (e.isIntersecting) {
                e.target.classList.add("is-visible");
                io.unobserve(e.target);
              }
            });
          },
          { rootMargin: "0px 0px -8% 0px", threshold: 0.12 },
        );
        els.forEach(function (el) {
          io.observe(el);
        });
      })();
    </script>
  </body>
</html>
