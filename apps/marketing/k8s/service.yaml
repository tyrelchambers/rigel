apiVersion: v1
kind: Service
metadata:
  name: helmsman-marketing
  namespace: default
  labels:
    app: helmsman-marketing
spec:
  selector:
    app: helmsman-marketing
  ports:
    - name: http
      port: 80
      targetPort: 80
