apiVersion: v1
kind: Service
metadata:
  name: rigel-marketing
  namespace: default
  labels:
    app: rigel-marketing
spec:
  selector:
    app: rigel-marketing
  ports:
    - name: http
      port: 80
      targetPort: 80
