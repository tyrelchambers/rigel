.superpowers/
.claude/settings.local.json

# Internal planning docs (repo-root only — must not match nested app dirs
# such as apps/marketing/src/content/docs or apps/marketing/src/pages/docs).
/docs/

# Node / pnpm
node_modules/

# macOS
.DS_Store

# TS build cache
*.tsbuildinfo

# build output
dist/
**/dist/

# Local secrets for docker compose (Claude OAuth token, etc.)
.env

# local visual-check scratch (playwright screenshots)
apps/web/.shots/

# Git worktrees (isolated workspaces)
.worktrees/
