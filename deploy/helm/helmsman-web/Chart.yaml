apiVersion: v2
name: helmsman-web
description: Helmsman — a self-hostable, AI-native Kubernetes admin UI (web). Runs in-cluster, drives kubectl/helm through a guarded confirm gate.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the app image; overridden per-deploy via image.tag.
appVersion: "latest"
keywords:
  - kubernetes
  - dashboard
  - admin
  - ai
home: https://github.com/cloudnative-pg
sources:
  - https://github.com/your-org/helmsman
