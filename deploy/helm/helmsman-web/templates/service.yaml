apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmsman-web.fullname" . }}
  labels:
    {{- include "helmsman-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helmsman-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
